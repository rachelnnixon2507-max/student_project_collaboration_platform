package com.project.platform.entity.enums;

/** Common project statuses shared across the whole platform. */
public enum ProjectStatus {
    DRAFT,
    OPEN,
    IN_PROGRESS,
    COMPLETED,
    APPROVED,
    REJECTED
}
